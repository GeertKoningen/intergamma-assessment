import * as WishlistItemModule from "@/components/WishlistItem";

describe("WishlistItem module", () => {
  it("is currently a placeholder module", () => {
    expect(Object.keys(WishlistItemModule)).toEqual(["default"]);
  });
});
