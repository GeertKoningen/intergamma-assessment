import * as WishlistDrawerModule from "@/components/WishlistDrawer";

describe("WishlistDrawer module", () => {
  it("is currently a placeholder module", () => {
    expect(Object.keys(WishlistDrawerModule)).toEqual(["default"]);
  });
});
